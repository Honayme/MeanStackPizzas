'use strict';

const { assert } = require('chai');

describe('testsBasiquesFromPizza !!!', (done) => {
    it('should equal', () => {
        assert.strictEqual(1, 1);
    });
    
});'use strict';
const { assert } = require('chai');

describe('testsBasiquesFromPizza !!!', (done) => {
    it('should equal', () => {
        assert.strictEqual(1, 1);
    });
    
});